# Review Fix Prompt

```text
请修复上一轮 Code Review 中的全部 P0 和 P1。

规则：

1. 不得扩大当前任务范围；
2. P2 只修复不会引入新范围的部分；
3. 每个修复必须对应具体 Review ID；
4. 为发现的 Bug 增加回归测试；
5. 不得降低断言或验收标准；
6. 更新 Task Plan 的 Review 结果和执行记录；
7. 更新 progress.md；
8. 重新运行：
   - 回归测试；
   - 当前模块测试；
   - 受影响的 Integration/E2E；
   - Ruff；
   - Mypy；
9. 输出问题到修复的对应关系；
10. 不执行 Commit。
```
